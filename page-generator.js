import { render } from "./z.js";

const genEventPage = (page) => [];

const genEventIndex = () => [];

const genAllEvents = () => [];

genEventIndex(events);

genAllEvents(events);

for(let evt of events) {

    genEventPage(events);

}
